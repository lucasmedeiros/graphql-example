type Query {
  hello(name: String!): String
  product(id: ID!): Product
  products: [Product]
}

type Mutation {
  addOrUpdateProduct(product: ProductInput!): Product!
  changeHello(newName: String!): String
}